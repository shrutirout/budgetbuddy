# Database Configuration
# For local development, use a local PostgreSQL instance
# For production, use a managed PostgreSQL service (Supabase, Railway, AWS RDS, etc.)

# Direct connection for Prisma Migrate / db push (port 5432)
# Use this URL when running: npx prisma migrate dev or npx prisma db push
# DATABASE_URL="postgresql://username:password@host:5432/database_name"

# Pooled connection for application queries (recommended for production)
# Using pooled connection for better performance and reliability
DATABASE_URL="postgresql://username:password@host:6543/database_name?pgbouncer=true&connection_limit=1"

# JWT Configuration
# Generate a strong random secret key (minimum 32 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your_random_secret_key_here_minimum_32_characters_change_this_in_production"

# Server Configuration
PORT=5000

# Google Gemini AI API Key
# Get your API key from: https://makersuite.google.com/app/apikey
# Used for AI-powered expense categorization and financial advisor chatbot
GEMINI_API_KEY="your_gemini_api_key_here"

# Node Environment (development or production)
NODE_ENV=development
